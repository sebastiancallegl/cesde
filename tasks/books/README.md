1. create a function that returns an array of books based in the availability status, the function should be named "getBooksByAvailability", the function should receive only one boolean parameter that is the availability that we want to get (true to get all available books and false to get the unavailable books)

2. create a function that returns an array of the books published in a given years range, the function should be named "getBooksForYearsRange", the function should receive 2 parameters (initial year and final year)
